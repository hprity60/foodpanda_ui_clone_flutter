import 'package:flutter/material.dart';

class PrtoductDetailPage extends StatelessWidget {
  const PrtoductDetailPage({super.key});
  static const id = "product_detail_page";

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
